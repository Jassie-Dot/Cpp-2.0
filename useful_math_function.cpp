#include<iostream>
using namespace std;
int main()
{
    double x=3;
    double y =4;
    double z;

    z=max(x,y); // This will tell which is maximum.
    cout<<z<<endl;
    z=min(x,y); // This tell which is minimum.
    cout<<z<<endl;

    return 0;
}